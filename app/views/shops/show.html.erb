<!DOCTYPE html>
<h1>ラーメン日記</h1>

<div class='block_show'>🍥<%= @shop.title %>🍥
  <div class= 'centering'><%= image_tag @shop.image.url.to_s %></div>
  <div><%= @shop.grade %></div>
  <div><%= @shop.comment %></div>
  <div class='Right_align'><%= link_to '編集', edit_shop_path(@shop) %>
  <%= link_to '戻る', shops_path %></div>
</div>
